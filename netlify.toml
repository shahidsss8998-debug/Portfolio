[build]
  command = "npm run build"
  publish = "dist"

# Redirect rule for single-page applications (in case React Router is added later)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
